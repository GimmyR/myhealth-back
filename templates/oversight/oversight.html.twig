{% extends "base.html.twig" %}

{% block title %}
	Oversight
{% endblock %}

{% block body %}

	{% if status < 0 %}
		{{ dump(message) }}
	{% endif %}

	{% if oversight is defined %}
		{{ dump(oversight) }}
	{% endif %}

	{% if chartDatas is defined %}
		{{ dump(chartDatas) }}
	{% endif %}

{% endblock %}
